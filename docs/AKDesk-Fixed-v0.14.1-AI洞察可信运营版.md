# AKDesk Fixed v0.14.1「AI 洞察可信运营版」

## 目标

让页面 AI 洞察从“能生成”走向“可查看、可计量、可清理”。

## 新增

- 按助手、市场、项目、信用和专题记录当日调用次数与 token；
- 页面卡片显示本模块用量；
- 用户可清除本机缓存的最终摘要；
- 生成中的摘要拒绝清除，避免队列状态与缓存竞争。

## 数据与隐私

SQLite schema 升级到 1410，新增 `ai_usage_events`。只记录日期、场景、次数与 token，不保存 API Key、完整提示词、思维过程或上下文正文。

