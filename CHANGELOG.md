# 更新记录

## v1.0.0 — 首个正式版

- 以 MIT License 公开源代码，新增中英文项目首页、贡献与支持指南、安全政策、行为准则、结构化 Bug / 数据源 / 功能建议模板、Dependabot 和 GitHub Actions；公开仓库只包含源码、预构建前端、正式文档与脱敏审计证据，排除本机数据库、备份、缓存、发布临时文件和凭据。
- 以 v0.18.0 的市场观察、固收投研、信用观察、全球数据和 BYOK AI 工作流为业务基线，不新增资产类别，不改变 SQLite schema 1600。
- v1.0.0-rc.1 的发布工程门禁全部通过后封版；正式版与候选版之间仅包含发布阻断修复、兼容性校准和文档收口。
- 将 NumPy 精确版本从 2.5.1 校准为 2.4.6，修复候选锁文件无法在 Python 3.11 安装的问题；Python 3.11、3.12、3.13 的独立环境均通过 149 项后端测试。
- 通过 70 项前端测试、Ruff、ESLint、CSS 令牌、TypeScript、Vite 生产构建、`pip check` 与 npm 生产依赖审计。
- schema 701、800、900、1100、1201、1301 的真实历史备份副本均可迁移到 schema 1600，并通过记录保留、数据库完整性、外键、备份恢复和损坏文件拒绝检查。
- 全新解压包在隔离 HOME 下完成首次安装、稳定数据目录、重复启动、停止脚本和真实 Control+C 验证；升级测试确认 rc.1 创建的研究项目可由 v1.0.0 沿用。
- 5 分钟稳态测试完成 59 轮核心 API 请求，无 HTTP 失败；数据库 `quick_check`、外键和 6 次自动备份均正常。
- 浏览器复核市场变化、每日复盘、每周复盘与外汇市场；1280×720、800×800 和 640×720 下无横向溢出或裁切，控制台无错误。
- 正式发布物包含文件级 `RELEASE_MANIFEST.json`、CycloneDX 1.5 `SBOM.cdx.json`、`THIRD_PARTY_NOTICES.md`、ZIP 校验值；排除用户数据库、缓存、备份、虚拟环境、前端开发依赖和 API Key。

## v1.0.0-rc.1 — 发布工程候选版

- 冻结 v0.18.0 已有业务范围，不新增行情源、资产类别或 AI 场景。
- 新增 `backend/requirements.lock`，以精确版本锁定完整 Python 生产依赖；macOS 启动器改用锁文件并通过 SHA-256 stamp 判断是否需要重新安装。
- rc.1 全新目录安装演练发现部分 python.org macOS Python 没有默认 CA 文件；启动器现在优先复用系统 Python 的 certifi CA，并在仍失败时给出 `Install Certificates.command` 恢复指引。
- 新增 `scripts/release.py`：构建前端、生成元数据、按白名单收集产品文件、校验版本一致性、Markdown 本地链接、启动脚本权限、用户数据排除和疑似凭据，并生成带文件级 SHA-256 的 `RELEASE_MANIFEST.json`、ZIP 包及 `SHA256SUMS`。
- 新增 CycloneDX 1.5 `SBOM.cdx.json` 和 `THIRD_PARTY_NOTICES.md`，区分软件许可证与外部数据 / API 使用条款。
- 新增 `scripts/check_migrations.py`，对 schema 701、800、900、1100、1201、1301 的真实历史备份副本执行迁移、记录保留、`quick_check`、外键、备份恢复和损坏备份拒绝门禁。
- 新增 `scripts/soak_test.py`，以隔离数据目录和演示模式持续访问核心 API，记录 RSS、线程、文件句柄、数据库和自动备份状态；服务端口支持通过 `AKDESK_PORT` 覆盖，正式启动器仍固定使用 8765。
- 全新发布包默认把运行数据放在 `~/Library/Application Support/AKDesk Fixed/`，使不同版本解压目录共享稳定的数据位置；已有项目检测到本地 `data/` 时继续使用旧路径，不静默搬移用户资料。
- 测试客户端改用 Starlette 当前推荐的 `httpx2` 后端，清除原有上游弃用告警；后端自动化增至 149 项，前端保持 70 项。
- 兼容矩阵发现 NumPy 2.5.1 不支持 Python 3.11；正式封版前将锁定版本校准为 2.4.6，并在 Python 3.11、3.12、3.13 分别完成全部后端测试。
- 发布包实测排除 `.venv`、`node_modules`、测试缓存、用户数据库、市场缓存、历史备份和密钥；候选包压缩体积低于 25 MB。

## v0.18.0 — 低门槛交付版

- 2026-07-28 市场变化中心视觉重构：首屏改为紧凑决策摘要，筛选收为单行工作栏，双列卡片改为全宽可核验信号流；来源、时点、质量与可信状态独立成证据区，并增加目标明确的模块核验动作。1440×900、1408×707 和 1024×768 复核均无横向溢出，详情见 [市场变化视觉重构复核](./docs/audits/v0.18.0-market-changes-redesign-20260728/REVIEW.md)。
- 2026-07-28 服务启停修复：新增 `stop-macos.command`。当服务由其他终端、IDE 或 Codex 会话启动时，不再要求用户找到原终端发送 `Control+C`；停止器会先核验 8765 端口返回的是当前 AKDesk 版本，再发送正常终止信号，避免误杀其他本地服务。启动器和用户手册同步说明 `Control+C` 只对原启动窗口有效。
- 2026-07-28 发布就绪复核：重新覆盖市场变化、每日复盘、外汇、研究首页、项目 / 专题空状态、数据健康、AI 助手和四类入口设置；1440×900 下无横向溢出、重复 ID 或破图，未发现新的阻断性功能缺陷。
- 正式数据库 `quick_check` 与外键检查通过；15 份现有 SQLite 备份逐一有效，schema 701、800、900、1100、1201、1301 的真实历史备份副本均可迁移到 1600。
- 后端 142 项、前端 70 项、Ruff、ESLint、CSS 令牌、TypeScript、生产构建、`pip check`、npm 生产依赖审计和启动脚本语法再次通过。结论与 v1.0 门禁见 [发布就绪复核](./docs/audits/v0.18.0-release-readiness-20260728/REVIEW.md) 和 [v1.0 发布就绪评估](./docs/AKDesk-Fixed-v1.0-发布就绪评估与规划.md)。
- 2026-07-28 界面细节修复：市场变化主摘要改为更稳健的标题字号、字重和宽度分配，三项指标与筛选文字不再因继承大字号而出现三行断字；1040 px 以下自动切换单列。
- 每周复盘的零值指标改用无斜线零的系统数字字体，底部研究声明收为内容宽度，不再形成空旷的通栏提示框。
- 外汇“数据边界”卡片不再被左侧图表强制拉高，图标与核心结论改为同一行，正文和列表补齐稳定内边距。
- 每日复盘的发行事件分组补齐标题、计数和事件列表的统一水平内边距，消除分组标题贴边与事件圆点错位。四处均按用户截图对应的 1408×707 逻辑视口复拍通过，证据见 [界面细节复核](./docs/audits/v0.18.0-ui-polish-20260728/REVIEW.md)。
- 2026-07-28 详细复核补齐 7 个遗漏的 CSS 设计令牌，修复市场变化卡片和研究行动卡片在部分区域丢失背景、边框与状态色的问题；同时移除 4 处 `color-mix()`，降低旧版 Safari 的样式兼容风险。
- 统一未知 `/api/*` 路径的行为：现在返回 JSON 404，不再被单页应用兜底误返回 `index.html`；后端测试已覆盖内容类型和错误信息。
- 修复重复点击当前导航或重复调用同一路由时继续写入浏览器历史的问题，避免“返回”需要多次操作。
- 提升入口设置、数据健康、页面洞察、AI 助手和关键操作控件的字号、对比度与点击高度；1440×900 复拍及浏览器控制台检查通过。
- 新增 CSS 自定义属性完整性检查并接入前端 `lint`，后续引用未定义设计令牌会直接阻断检查。详细证据见 [v0.18.0 详细复核](./docs/audits/v0.18.0-detailed-20260728/REVIEW.md)。
- 首次启动从“两种产品模式”升级为四类任务入口：每日看市场、利率与宏观研究、信用观察、全球跨市场；选择只改变默认落地页和导航优先级，不隐藏功能、不拆分数据库。
- 入口设置同步支持四类场景，并增加“检查数据连接”和“AI 与隐私设置”直达入口。
- 保留顶部“市场 / 投研”临时切换；修复会话模式与默认场景同时存在时的启动落点冲突，以及浏览器前进 / 后退状态同步。
- 完成 1440×900 MacBook Air 主流程审查；首次入口、市场变化、研究行动、专题综述和入口设置均已截图取证。
- 最终后端 142 项、前端 70 项测试通过；Ruff、ESLint、TypeScript、生产构建、Python 依赖、npm 生产依赖审计及正式 SQLite 完整性 / 外键检查通过。

## v0.17.0 — 跨源专题综述版

- 专题投研新增独立 BYOK AI 综述卡片，输出“共同变化 / 来源分歧 / 证据缺口 / 下一步核验”。
- 专题 AI 权限独立于市场、项目和信用，默认关闭，支持手动、数据变化后或每日首次打开。
- 外发上下文只含专题元数据、组件类型、最多 8 个项目的标题级摘要及 20 条时间线元数据；不发送项目记录正文、新闻正文、完整结论或组件查询值。
- 最终摘要继续只缓存于本机，完整提示词、思维过程和上下文副本不落库。

## v0.16.0 — 研究行动助手版

- 研究项目新增行动助手，根据本地项目缺口和已缓存 AI 洞察整理任务、反证和复盘草稿。
- 建议不会直接改项目；只有用户勾选并再次确认后才写入研究记录，已采纳建议可撤销。
- 每条建议展示来源与引用边界；重复采纳、跨项目草稿和重复撤销均有事务级保护。
- SQLite schema 从 1410 升级至 1600，新增 `research_action_drafts` 和外键约束。

## v0.15.1 — 市场可信校准版

- 市场变化中心增加“仅显示可信可行动”、最低质量分和高优先 / 关注 / 信息三级过滤。
- 增加“标记本次已读”，在浏览器本地保存当前信号指纹；不会改写研究库。
- 变化卡片统一展示来源、观测时间、抓取时间、质量分、可信级别、阈值理由和原始模块入口。

## v0.15.0 — 市场变化中心版

- 新增市场变化中心，把上一收盘以来的变化和上次查看后的新增 / 变化信号放在同一页。
- 聚合资金、曲线、国债期货和现券信号，按可行动性、严重度、质量分和稳定 ID 排序。
- 根据交易时段给出盘中核验或收盘复盘提示；市场和投研两种导航均可访问。

## v0.14.1 — AI 洞察可信运营版

- 页面洞察支持清除本机最终摘要缓存，不删除业务数据；生成中的摘要禁止清除。
- AI 用量从每日总量扩展为按助手 / 市场 / 项目 / 信用 / 专题场景统计，页面卡片展示本模块当日次数和 token。
- SQLite schema 从 1301 升级至 1410，新增 `ai_usage_events`；不保存 API Key、完整提示词或上下文正文。
- 修复 AI 用量记录的场景归属和缓存清理接口边界。

## v0.14.0 — 场景化 AI 洞察版

- 今日市场、研究项目和信用观察 R2 新增内嵌 AI 洞察卡片，分别输出“变化线索 / 可信边界 / 下一步核验”“当前证据 / 关键缺口 / 下一步验证”和“新增信号 / 数据缺口 / 人工复核任务”。
- 三类页面洞察共用 v0.13.1 的独立授权与每日次数、token 双预算；所有模块默认关闭，授权后可选择仅手动、数据变化后或每日首次打开。
- 新增稳定上下文指纹、过期识别和最终摘要缓存；页面数据变化时旧摘要保留并标为待更新，完整提示词、思维过程和上下文副本不写入 SQLite。
- 新增单工作线程、最多 10 个任务的本机队列；生成中持续轮询，刷新时保留上次摘要，上游成功但没有最终回答时计入实际用量并安全重试一次。
- 页面卡片增加发送范围预览、失败重试、权限设置定位和“继续追问”；追问只把固定场景问题带入原有不落库助手，不保存多轮历史。
- 信用洞察不生成评级、违约概率或投资结论，项目洞察不改写用户结论，市场洞察只使用最多 8 条带来源和可信状态的裁剪线索。
- SQLite schema 保持 1301；正式库完整性、外键和 Key 泄漏检查通过，页面洞察表只含最终摘要及生成元数据。
- 后端 139 项、前端 66 项测试通过；Ruff、ESLint、TypeScript、生产构建、Python 依赖和 npm 生产依赖审计通过。

## v0.13.1 — AI 运行底座版

- AI 助手新增每日调用次数和 token 双预算，默认分别为 20 次与 100,000 tokens；成功问答计入当日汇总，连接测试不计费，达到任一上限后明确拒绝新请求。
- 新增今日市场、研究项目和信用观察三个模块的独立 AI 洞察授权，默认全部关闭；用户可选择仅手动、数据变化后或每日首次打开触发。
- 顶部 AI 入口增加未配置、可用、生成中和预算耗尽状态，抽屉内显示今日用量、剩余额度、单并发和任务队列状态。
- SQLite schema 从 1201 升级到 1301，新增 `ai_usage_daily` 和 `ai_insights`；只保存每日计数及页面最终摘要、上下文指纹和生成元数据，不保存 Key、完整提示词、思维过程、附件正文或新闻正文。
- 对话问答继续保持请求级、不落库；页面洞察采用独立缓存边界，三个业务页面在 v0.14.0 接入。
- 正式库升级前已创建并校验 `data/backups/before-v0.13.1-20260727.db`。
- 后端 134 项、前端 64 项测试通过，Ruff、ESLint、TypeScript、生产构建和 SQLite 迁移检查通过。

## v0.13.0.1 — AI 连接与呈现修复版

- 修复本地服务停止后浏览器只显示英文 `Failed to fetch` 的问题；所有 API 网络级失败现在统一提示重新启动 macOS 本地服务，并与上游 AIHubMix 错误分开呈现。
- AIHubMix 调用改为可取消的异步请求，增加 75 秒总时限以及 30 秒单次读取时限；即使上游持续分块或网络代理异常，也不会让浏览器无限等待。
- AI 前端增加 85 秒兜底时限，超时后明确说明本次问答未保存；401 / 404 / 429 / 额度不足和连接失败继续保留独立提示。
- 修复长回答被压缩到约 93 px、正文实际返回但不可见的问题；AI 抽屉改为内容自然高度和内部滚动，完整回答可浏览。
- 回答区安全呈现加粗、标题、编号和项目符号等常见 Markdown，不使用 `dangerouslySetInnerHTML`，并把正文字号从 10 px 提升到 11 px。
- 使用用户 Keychain 凭据完成真实 AIHubMix 连接和产品向导问答回归；1280×800 无横向溢出，浏览器控制台无错误。
- SQLite schema 保持 1201，不迁移、不写入 AI 问答；后端 131 项、前端 64 项测试通过，Ruff、ESLint、TypeScript 和生产构建通过。

## v0.13.0 — 低门槛与 AI 助手预览版

- 顶部工具栏新增全局 AI 研究助手抽屉，提供“产品向导、市场观察、研究教练”三类场景和可直接采用的问题建议；用户无需学习 Prompt 工程即可开始。
- 接入 AIHubMix OpenAI 兼容 Chat Completions，固定使用 `deepseek-v4-flash-think`（DeepSeek V4 Flash Thinking）；不允许前端切换到未验证模型。
- 支持用户自己的 AIHubMix API Key：验证成功后只写入 macOS Keychain，也可通过 `AKDESK_AIHUBMIX_API_KEY` 环境变量提供；状态接口只返回是否配置和来源，不返回明文。
- 每次 AI 调用都展示发送前预览并要求显式确认；产品帮助只发送问题、当前页面和模块地图，市场场景只发送最多 8 条线索及来源 / 时点 / 可信状态，研究场景不发送研究记录正文、附件、数据库或完整行情表。
- AI 回答采用请求级处理，不写入 SQLite、日志、浏览器缓存、研究项目或专题；关闭抽屉后清除当前回答。只展示最终回答，不展示模型内部思维过程。
- 后端增加 90 秒超时、96 KB 响应上限、14,000 字符上下文上限、单请求并发门，以及 401 / 404 / 429 / 额度不足等错误的安全映射；错误信息不包含 Key 或上游响应正文。
- AI 系统提示强制区分观察、推断和待验证事项，禁止生成买卖、仓位、收益承诺、自动评级或违约概率，且不会自动改写任何研究资产。
- SQLite schema 继续保持 1201，不迁移、不改写正式研究库；后端 130 项、前端 62 项测试通过，Ruff、ESLint、TypeScript 和生产构建通过。

## v0.12.2 — 低门槛体验版

- 今日市场与研究首页新增状态驱动的四步起步清单：根据数据源、自选、最近访问、项目、专题、证据篮和待复盘状态自动标记完成项，并把异常或到期事项提升为待处理。
- 全局搜索新增“今天市场怎么样、开始一个研究、整理一个专题、为什么数据不可用、跟踪信用债、做每周复盘”等自然语言式任务入口；本版本只做本地意图映射，不调用外部大模型。
- 首次入口说明补充“检查数据 → 建立自选 → 创建项目或专题 → 保存证据并复盘”的推荐路径；清单可按市场 / 投研模式分别收起，偏好只保存在浏览器。
- 专题工作台新增专题、项目、证据与复盘关系说明，以及幂等的“中国利率示例专题”入口；已有同类专题时直接打开，不重复制造研究对象。
- 研究项目优先展示模板立项，自定义空白项目与导入 / 备份 / 恢复改为渐进式展开；信用观察把主体属性、回售与复盘日期收入高级区，核心校验字段保持前置。
- 统一错误状态增加明确的重试建议与数据健康入口，减少“数据不可用”后的操作断点。
- SQLite schema 继续保持 1201，不迁移、不改写正式研究库；后端 125 项、前端 60 项测试通过，Ruff、ESLint、TypeScript 和生产构建通过。

## v0.12.1.1 — 信用滚动修复版

- 修复信用观察长列表深处切换“单券 / 发行人 / 自定义组合”后停在页面底部的问题；页签切换后会把新面板稳定定位到粘性工具栏下方。
- 修复深滚动后搜索或观察状态筛选使结果缩短、但匹配结果仍位于视口外的问题；筛选变化会同步复位结果区和分段数量。
- 重新以主滚动容器为坐标系校准粘性工具栏，移除重复的 50 px 顶部偏移，并为主内容、侧栏和组合成员区保留稳定细滚动提示。
- 单券列表改为首批 20 条、每次再显示 20 条；42、100 和 500 只规模下首屏 DOM 均保持 20 张单券卡片，风险信号与日历不再被完整长列表无限推后。
- 自定义组合新增成员搜索、“仅看已选”和粘性保存操作区；在大成员集下仍可快速定位已选债券。
- 信用三级页签补齐 roving `tabIndex`、方向键、Home 和 End 键盘模型，焦点与选中状态同步。
- SQLite schema 继续保持 1201，不迁移、不改写正式研究库；隔离数据库完成 42 / 100 / 500 只回归，1440×900 与 1280×800 无横向溢出，浏览器控制台无告警。
- 后端 125 项、前端 57 项测试通过；Ruff、ESLint、TypeScript 和生产构建通过。

## v0.12.1 — GDELT 事件雷达

- 专题组件新增 GDELT DOC 2.0 事件雷达，支持 1–90 天、5–50 条、最新或相关性排序的按需查询；展示标题、原文链接、来源、国家、语言和本机时间。
- 事件结果明确标为“待核验线索”，单条选择后先进入证据篮，只有用户提交到研究项目后才进入专题时间线；不生成事实确认、情绪分数、信用评分或投资结论。
- 信用观察 R2 的发行人主档新增事件观察入口，以主体归一名创建或复用唯一专题，连接发行人、事件线索、证据篮和研究项目。
- 新增 12 小时 GDELT 元数据缓存、至少 5 秒请求间隔、单次 50 条和 512 KB 边界、失败缓存回退与连续失败熔断；不抓取或保存新闻正文。
- SQLite schema 从 1100 升级到 1201，新增 `gdelt_query_cache`；正式库升级前已生成并验证 `data/backups/before-v0.12.1-20260723.db`。
- 发布前整体评审修复键盘跳过侧栏入口、GDELT 本机时间口径和证据篮空状态说明；连接器与事件主流程在 1440×900 完成回归。
- 后端 125 项、前端 52 项测试通过；Ruff、ESLint、TypeScript、生产构建、SQLite 完整性和迁移检查通过。

## v0.12.0 — 统一外部数据连接器

- 数据健康中心新增统一连接器总览，以相同字段展示 AKShare / AKTools、FRED、World Bank 和 GDELT 的访问方式、凭据状态、能力、本地保存、缓存、许可、署名、请求策略和能力边界。
- 新增 `GET /api/v1/connectors`，只返回凭据是否配置和来源，不返回 API Key 明文；逐适配器 `sources/health` 继续负责运行诊断，两层语义不混淆。
- 清缓存确认文案明确列出中国行情、World Bank 与 GDELT 缓存，并说明不会删除项目、证据、自选、提醒或复盘归档。
- v0.12.0 本身不迁移 schema；其连接器契约作为 v0.12.1 GDELT 和后续可选外部数据提供方的统一治理底座。

## v0.11.1 — 信用研究联动版

- 发行人主档新增一键研究立项，以主体归一名创建“信用债覆盖与利差观察”项目；相同主体和模板重复操作会打开已有未归档项目，不制造重复研究。
- 信用规则信号的人工确认增加核验备注和可选研究项目；保存后可自动生成一条已完成的项目复盘记录，保留信号内容、主体、债券、观察日期、来源和人工备注。
- 信号确认仍可只在信用观察中完成，不强制关联项目；已经确认的信号不会因重复请求再次写入研究记录。
- 新增“导出多期历史”CSV，按债券和观察日期从早到晚导出全部本地信用快照；“导出当前观察”继续只导出每只债券的当前主档。
- 研究项目模板对象类型扩展到发行人，但自选标的类型没有扩大；信用研究联动不会把发行人静默加入行情自选。
- SQLite schema 继续保持 1100，无新增迁移、无正式研究数据改写。
- 后端 118 项、前端 52 项测试通过；Ruff、ESLint、TypeScript、生产构建、依赖审计、SQLite 完整性、Markdown 本地链接和 1440×900 浏览器主流程回归通过。

## v0.11.0.1 — 信用可信与工作流修复版

- 完成 v0.11.0 的代码、迁移、接口、1440×900 页面与核心工作流复核；市场/投研双入口、研究项目、数据健康和 schema 1100 未发现阻断性回归。
- 修复 CSV/Excel 中空白“基准期限”被解析为空字符串、导致本应允许的缺字段记录整批无法确认导入的问题；空白可选枚举现在保留为 `null`，并按既有门禁显示“不计算”。
- 信用观察改为“先观察、后维护”：单券/发行人/组合视图前置，导入与编辑默认收起，风险信号移到观察结果之后，MacBook Air 首屏不再被长表单占满。
- 发行人和自定义组合不再展示跨不同债券的简单平均机械利差，改为显示通过门槛单券的利差范围与样本数，避免把异质债券聚合成伪精确结论。
- 新增“导出当前观察”CSV，字段与标准导入模板一致，可直接回环导入；完整历史、组合和信号确认仍通过研究库备份保留。
- 信用页补齐原生页签语义、选中状态、搜索名称和唯一信号确认名称；信用日程统一按上海自然日计算。
- SQLite schema 继续保持 1100，无新增迁移、无现有研究数据改写。

## v0.11.0 — 信用观察 R2

- 信用观察从单笔机械利差试算升级为单券、发行人和自定义组合三级工作台；主体按人工维护的归一名聚合，不生成自动评级或主体评分。
- 新增 CSV、Excel 批量导入和字段映射预览，支持 UTF-8/GB18030 CSV 与 `.xlsx/.xlsm`；导入前逐行校验，缺少来源、日期或基准的记录保留为空并阻断机械利差。
- 新增信用历史快照：同一债券可保存多期收益率、评级和基准记录，展示历史期数、最新利差、较上期变化和本地历史分位；导入旧数据不会覆盖更新的当前快照。
- 新增发行人主档聚合与自定义观察组合，汇总债券数、评级记录、机械利差和下一事件；组合只做观察归集，不计算仓位、收益或信用评分。v0.11.0.1 起聚合视图只显示单券范围与样本数，不再计算跨券简单平均值。
- 新增到期、回售、评级资料复核和研究复盘日历；规则信号保留债券、主体、观察日期、来源和质量状态，必须由用户手工确认。
- 单券继续沿用同日、同期限和完整来源门禁，可一键创建信用研究项目；自动候选仍只读取本机可信且未过期的现券和国债曲线缓存。
- SQLite schema 从 900 升级为 1100，新增信用历史、组合成员和信号确认表；迁移幂等保留旧版信用观察、自选、提醒、专题、项目、证据和宏观缓存。
- 后端 114 项、前端 52 项测试通过；ESLint、Ruff、TypeScript、生产构建、依赖审计、真实 SQLite 迁移和 MacBook Air 浏览器回归通过。

## v0.10.0 — 市场驾驶舱增强版

- 今日市场升级为可配置驾驶舱：收益率曲线、国债期货、活跃现券、重要事件和跨市场外汇可按个人习惯显示或隐藏，并提供“全景、利率优先、跨市场”三套本地预设。
- 新增最近查看的市场模块入口；偏好仅保存在当前浏览器本地，不进入研究库，也不影响市场/投研双入口的数据共享。
- 新增利率与汇率联动观察：用本地可信历史计算 10Y 国债和 FDR007 分位，联动展示 10Y–1Y 曲线、T 主力与 USD/CNY 方向；样本量随卡片展示，缺失时不伪造分位。
- 市场驾驶舱增加“进入投研工作台”入口，把市场观察继续沉淀为现有的项目、证据和复盘流程；没有新增自动投资结论或交易功能。
- 完成 MacBook Air 紧凑布局、键盘焦点和窄屏适配；最终版本号、macOS 启动器、产品说明和用户手册统一升级至 v0.10.0。
- 后端 110 项、前端 48 项测试通过；ESLint、Ruff、TypeScript、生产构建、Python 依赖与真实 SQLite 完整性检查通过。SQLite schema 继续保持 900，无迁移、无研究数据改写。

## v0.9.3 — 研究工作流收口版

- 投研首页新增“发现问题 → 建立项目 → 保存证据 → 更新观点 → 定期复盘”五步导航，并依据本地项目、证据、结论和到期状态提示当前步骤。
- 研究项目新增六项闭环完成度和明确的下一步建议；编辑器默认突出标题、问题、状态、置信度和结论，假设、期限、标签与复盘日期收进渐进式高级区。
- 专题工作台把日常阅读与低频设置分开：阅读时专注组件、时间线和证据篮，需要时再打开专题设置；无专题时解释专题、项目和证据篮之间的关系。
- 每周复盘补齐本周观点、下周待验证和下次复盘，默认周结束日明确为上海当前日期，Markdown 导出同步包含新增段落。
- 投研概览补充已有结论和已保存证据数量，不迁移、不重写既有专题、项目、证据或时间线。

## v0.9.2.2 — 可信呈现修复版

- 折线图坐标轴与悬浮值改为按实际数值区间动态确定 0–5 位小数，窄幅利率和汇率变化不再因固定精度显示成“没有变化”。
- 数据健康中心把研究语义明确为“当前已验证、可信缓存可参考、禁止沉淀为证据、尚未验证”，并解释连接状态与研究可用性的区别。
- 新增研究库后台自动安全备份：启动时检查、默认每 24 小时创建经 SQLite 完整性验证的原子快照、滚动保留 14 份；项目页显示状态并支持立即备份。
- 自动备份失败只记录状态，不阻断本地服务启动；原有手动下载备份、恢复前安全快照和项目 JSON 导入导出继续保留。
- SQLite schema 保持 900，无迁移；升级前已额外生成并校验真实研究库安全副本。

## v0.9.2.1 — 可信交互收口版

- 专题时间线改为稳定游标分页：工作区首屏读取最近 100 条，明确显示已加载数量、完整总数和剩余范围，可按需加载更早记录；旧版列表接口保持兼容。
- 专题 Markdown 改为导出完整时间线，并在文件头写明完整记录数，不再静默截断第 201 条之后的研究历史。
- 本地历史没有有效数值时显示可操作的空状态，不再渲染“0 个时间点”的空白图；所有折线图同步提供最新值、观察期、区间变化和可展开数据摘要。
- 增加全局界面错误边界，图表懒加载或页面渲染失败时明确说明本地数据未被删除，并提供重新加载入口。
- 侧栏当前页面增加 `aria-current`；提高辅助文字字号、弱化文字对比度和数据健康元信息可读性，保持 MacBook Air 紧凑布局。
- 新增 ESLint、Ruff 正确性检查、React 组件错误恢复测试、图表摘要测试、游标分页与 226 条完整导出回归；修正信用观察候选带入函数的 Hook 误命名。
- 版本号、macOS 启动器、产品介绍和用户手册升级至 v0.9.2.1；SQLite schema 继续保持 900，无迁移、无研究数据改写。
- 后端 108 项、前端 42 项测试通过；ESLint、Ruff、TypeScript、生产构建、Python 依赖检查、npm 全依赖审计、27 份 Markdown 本地链接和真实 SQLite 完整性检查通过。

## v0.9.2 — 专题规模与交互增强版

- 专题库列表改为一次读取专题与研究对象、一次批量读取全部项目关联，消除专题数量增长时的逐专题数据库连接；新增标题、问题、说明、标签、研究对象搜索，状态筛选和最多 500 条读取边界。
- 专题页面新增本地即时搜索和状态筛选，显示匹配数量并区分“尚无专题”与“没有符合条件的专题”；50 个专题规模回归确认列表读取只使用一个 SQLite 连接。
- 市场脉搏、本地历史、FRED 和 World Bank 专题组件新增独立刷新/重试按钮与加载状态；强制刷新安全绕过短时内存缓存，旧请求不会覆盖更新后的共享缓存。
- 跨源研究时间线新增文本、记录类型和关联项目筛选，每次显示 20 条并按需继续加载；专题聚合读取上限扩展到 300 条，长时间线不再一次铺满页面。
- 专题列表补齐原生按钮语义、当前选择状态、列表角色与结果播报；切换专题后焦点进入专题标题，时间线补齐列表语义和筛选结果播报。
- 版本号、macOS 启动器、产品介绍和用户手册升级至 v0.9.2；SQLite schema 保持 900，无需迁移。
- 后端 107 项、前端 37 项测试及生产构建通过；完成 50 专题规模、共享缓存强制刷新、专题筛选和时间线筛选回归。

## v0.9.1 — 专题可用性增强版

- 新增专题工作区聚合接口，一次返回专题详情、跨源时间线和全部关联项目摘要，替代“详情＋时间线＋逐项目摘要”的 N+1 前端请求。
- 专题的市场脉搏、本地历史、FRED 和 World Bank 组件改为进入视口前 240 px 才加载；相同读取在请求进行中自动合并，并短时复用结果，减少多组件与多专题重复请求。
- 专题组件支持直接编辑标题和数据配置：本地历史可改来源、指标、序列、天数和单位；FRED 可改序列、年限和变换；World Bank 可改经济体、指标、年度与比较方式。后端同步增加逐类型配置校验。
- 证据篮新增按当前专题、未归属、来源和起始日期筛选，支持全选筛选结果和最多 100 项批量清理；批量删除继续安全清理无引用研究对象。
- 现券对“YY附息国债NN、YY国开NN、YY进出NN、YY农发NN”使用保守标准简称规则恢复代码，并明确标为“规则推导、需核验”；未核验代码不允许直接加入自选或创建项目，其他债券继续保持代码为空。
- 可转债新增“完整比价 / 仅价格”数据层级和核心字段覆盖门禁；新浪备用行情只保留观察与自选能力，不允许用仅价格记录直接研究立项，CSV 同步导出数据层级。
- 版本号、macOS 启动器、产品文档和用户手册升级至 v0.9.1；SQLite schema 保持 900，无需迁移。
- 后端 106 项、前端 34 项测试及生产构建通过；完成真实数据库、专题聚合主流程和行情质量校准。

## v0.9.0.1 — 连接与长稳修复版

- 修复主研究库和持久行情缓存的 SQLite 连接生命周期：每次读写、初始化、备份、恢复和完整性校验结束后均确定性关闭连接，不再依赖 Python 垃圾回收释放文件句柄。
- 修复长时间运行后文件描述符持续累积、最终可能导致缓存读取与数据源状态误报“不可用”的 P1 稳定性问题；市场休市仍只影响数据时效标签，不会被当作连接失败。
- 新增禁用垃圾回收条件下的文件描述符稳定性回归，连续执行 240 次主库与缓存查询后句柄数保持稳定。
- `/api/v1/info` 新增进程、运行时长、线程数、文件句柄数量与软硬上限诊断；数据健康中心同步展示本机运行资源，便于判断数据源故障与本机资源问题。
- 全局健康状态和未读提醒在页面进入后台后暂停轮询，回到前台立即同步；数据健康中心轮询由 5 秒调整为 15 秒，减少 MacBook Air 长时间后台运行时的无效请求与唤醒。
- 版本号、macOS 启动器、前端资源、产品介绍、用户手册和版本文档统一升级至 v0.9.0.1；SQLite schema 保持 900，无需数据迁移。
- 后端 101 项、前端 30 项测试及生产构建通过；完成禁用 GC、低文件句柄上限和真实运行进程的长稳压力验证。

## v0.9.0 — 专题投研版

- 新增“专题投研工作台”，以利率主题、宏观主题、国家、发行人、证券和数据发布事件六类统一研究对象组织市场、宏观、主权和项目资料。
- 内置中国利率方向、中美利率联动、人民币汇率与利率、全球通胀与实际利率、空白专题五类模板；支持保存市场历史、FRED 图表、World Bank 比较、项目摘要和最近证据组件。
- 新增跨源研究时间线，统一展示市场快照、FRED 引用、World Bank 快照、笔记、反证、任务、发布复盘和观点字段变化，同时保留记录时间与来源观察时间边界。
- 新增证据篮；研究首页市场快照、图表研究室 FRED 引用和主权比较可先收集，再批量提交到研究项目并关联当前专题与研究对象。
- 新增规则化项目摘要，整理问题、假设、结论、支持证据、反证、未完成任务、观点变化、复盘日期和字段缺口，不调用生成式 AI。
- 专题支持全局搜索、项目关联、组件管理和 Markdown 导出；项目 JSON 导入导出同步保留研究对象关联。
- FRED 继续只保存引用配置，不允许证据篮绕过观测值落盘校验；World Bank 和市场快照继续沿用既有许可与可信门禁。
- SQLite schema 升级至 900，新增专题、研究对象、项目/证据对象映射和证据篮表；v0.8.2 数据自动幂等迁移并保留既有研究资料。
- 完整评审修复专题深链回跳、跨专题证据串线、证据篮来源/目标对象归属、项目包导入对象关联、无引用研究对象清理和旧版 Safari 兼容问题。
- 在 8 GB Apple Silicon MacBook Air 上完成真实数据库迁移、临时专题端到端回归和 FRED / World Bank / 本地曲线联调；后端 99 项、前端 29 项测试及生产构建通过，详见 [v0.9.0 完整评审](./docs/review-v0.9/AKDesk-Fixed-v0.9.0-完整评审.md)。

## v0.8.2 — 市场增强版

- 新增“外汇市场”，采用人民币日频参考价与全球外币对即时询价两条独立 AKShare 链路，分别缓存、刷新、熔断和展示健康状态。
- 人民币参考价首期覆盖 USD/CNY、EUR/CNY 和 100 JPY/CNY，展示央行中间价、中行现汇买卖价、日变化及最近公开历史；严格排除没有央行中间价的周末折算记录。
- 全球外币对展示买报价、卖报价、中间值和点差；源未提供可信市场时间时明确标为部分可用，不写入研究历史，也不开放自动提醒。
- “今日市场”升级为六项跨市场脉搏，并新增跨市场外汇区；外汇标的进入全局搜索、统一自选和研究项目关联流程。
- 顶部市场时钟按页面隔离中国固收和全球外汇状态，避免工作日全天外汇交易导致中国市场收盘状态被误报。
- 不新增数据库表，SQLite schema 保持 800；后端 90 项、前端 26 项回归及生产构建通过。

## v0.8.1 — 双入口体验版

- 新增首次使用入口选择，用户可以将“市场模式”或“投研模式”设为默认入口；两种模式共享同一套 SQLite、自选、提醒、项目和研究证据。
- 顶部新增常驻“市场 / 投研”切换器和入口设置；支持修改下次启动的默认模式、重新查看入口说明，并使用 `Command + 1` 返回当前模式首页。
- 市场模式默认进入“今日市场”，导航优先展示今日、自选、中国固收和全球市场；投研模式默认进入“研究首页”，导航优先展示项目、数据研究和研究复盘。
- 从两个首页切换模式时进入对应首页；在曲线、现券、宏观等专业页面切换时保留当前页面和筛选状态。
- 全局搜索与深链继续覆盖全部功能，不因当前模式隐藏结果；“固收市场”页面明确调整为“今日市场”，与研究首页的职责进一步分离。
- 入口偏好只保存在浏览器本地，不进入研究数据库或备份；SQLite schema 保持 800，既有研究资料无需迁移。
- 后端 85 项、前端 22 项回归及生产构建通过。

## v0.8.0 — 主权比较版

- 新增“全球主权比较”模块，接入无需 API Key 的 World Bank Indicators API v2；首期覆盖 10 个经济体和 10 条 WDI 精选指标，聚焦增长通胀、财政债务、外部平衡与收入结构。
- 单次支持 2–8 个经济体、最长 51 年的年度比较；严格按同一年度对齐，缺失值保持为空，不做跨年填充，提供水平值、单年变化、横截面数值排名和 Z-Score。
- 页面展示数据覆盖率、最新共同年份、最新可用年份、指标口径、官方更新时间、原始机构、来源、CC BY 4.0 许可和署名；CSV 导出保留来源与许可说明。
- 新增“主权基本面比较”研究模板；可从比较页一键立项，并将可信 World Bank 比较结果连同原值、年份、元数据和许可保存为本地研究证据。
- World Bank 使用独立 SQLite 持久缓存，完整记录缺失占位并以 7 天为复用周期；官方刷新失败时明确使用过期缓存，不把旧数据显示成最新数据。
- 新增 World Bank 独立健康状态、HTTP/2 网络连接、直连优先与系统代理回退、单源重试、三次失败熔断、缓存统计和清理入口；全局搜索可直接定位精选主权指标，投研首页展示 World Bank 缓存点数。
- FRED 继续维持“用户 Key + 请求级观测值 + 引用证据”的边界，不因 World Bank 可持久化而扩大 FRED 落盘范围。
- 产品介绍和用户手册同步更新至 v0.8.0，并新增功能布局、目标用户、业务场景与后续版本路线分析。
- SQLite schema 升级至 800，新增 World Bank 国家、指标和观测表并保留既有研究数据；后端 85 项、前端 17 项回归及生产构建通过。

## v0.7.0.1 — 可信收口版

- 信用行情候选新增严格来源门禁：仅接受未过期、非演示、具有观察日、可信等级为 `trusted` 且质量分不低于 80 的本地缓存；现券与国债曲线分别展示放行状态和阻断原因。
- FRED 研究证据校验从“来源文字前缀”升级为结构化识别；来源摘要、provider/source/adapter 和官方 URL 任一指向 FRED 时，均强制 `reference_only`，并递归拒绝观测值字段。
- 一键立项新增项目来源映射。同一模板、对象类型与对象标识重复点击会返回既有未归档项目，避免重复建项；创建后可直接打开对应项目。
- 数据发布复盘支持编辑、复制和取消编辑；选择 FRED 序列时辅助填入名称、单位并显示频率，空清单增加明确的使用引导，异步操作补齐错误反馈。
- 信用观察支持编辑已核验字段，覆盖率与缺失项改用中文字段名；空清单不再展示没有意义的 `0/0` 覆盖卡片。
- 研究记录类型、隐藏导入控件和敏感度设置补齐可访问名称；自选行情状态、可信等级与设置字段完成中文化。
- 新增 v0.7 核心接口的 HTTP 集成回归，覆盖模板幂等、研究记录、信号设置、自选、周报、项目导入导出、整库备份恢复、发布复盘和信用观察。
- SQLite schema 升级至 701，只新增项目来源映射并保留既有数据；后端 79 项、前端 16 项回归和生产构建通过。

## v0.7.0 — 整合研究版

- 合并原 v0.6.2“研究生产力增强”、v0.6.3“数据发布复盘”和 v0.7“信用观察 R1”的规划范围，形成行情发现、立项、记录、复盘与覆盖校验的一体化版本。
- 统一自选升级为行情增强自选：证券自选显示当前缓存行情、涨跌、来源、可信等级、观测时间和详情深链；宏观自选明确提示按需请求，不在后台预抓 FRED。
- 现券、国债期货、可转债、市场研究线索和信用观察均可一键创建研究项目；新增空白、利率方向、宏观发布、转债事件和信用观察五类模板。
- 研究项目新增研究笔记、反证、任务与复盘记录，支持完成状态和到期日期，并把新增、更新、移除写入观点轨迹。
- 研究项目支持不含 FRED 观测值的 JSON 项目包导入/导出、Markdown 导出；研究数据库支持 SQLite 下载备份、完整性校验恢复和恢复前安全副本。
- 市场研究线索阈值改为本地可配置，资金、曲线、期限利差、期货和现券线索均显示具体触发条件与原因。
- 新增每周研究复盘，汇总项目更新、笔记、反证、任务、发布复盘和到期项目，支持 Markdown 导出。
- 新增数据发布复盘：保存用户发布前预期，按需请求 FRED 官方实际值，计算预期偏差，记录已复盘 vintage，并展示中国资金、10Y、期限利差与 T 合约本地前后窗口。
- FRED 实际值继续只在单次请求内处理；SQLite 仅保存用户预期、复盘备注和 vintage 引用，不保存官方观测值。
- 信用观察升级为 R1 覆盖门禁：标准化发行人显示，校验到期日、评级及日期、收益率及日期、同日基准与期限匹配；任何关键字段或日期不合格时拒绝计算，仅合格对象机械试算 `债券收益率 - 同期限国债基准`。
- 信用模块明确不生成内部评级、违约概率或投资结论；不稳定公开信用接口仅作为已验证行情候选辅助，不被包装成可信评级源。
- SQLite schema 升级至 700，新增研究记录、发布复盘、信用观察、应用设置和迁移记录；升级过程保留既有研究、自选、提醒、日报与市场历史。
- 后端回归增至 77 项，前端回归保持 16 项；生产构建通过。

## v0.6.1.1 — 可信修复版

- 市场状态机新增未开盘与午间休市，和交易中、已收盘、非交易日、日频数据明确区分；宏观页面顶部改为“按发布更新”，不再被中国市场收盘状态干扰。
- 国债期货不再把缺失交易日的 15:15 快照拼接为本地当天；优先使用源交易日或交易日历确认的最近已发生时点，进入缓存和写入历史前均拒绝未来时间。
- 升级启动时幂等清理未来日期和盘前误写的当日国债期货历史点，并通过 `/api/v1/info` 返回本次清理数量。
- 数据健康新增“研究可用、受限可用、不可用于研究、待检查”，与连接健康、缓存和降级状态分开统计；演示或不可用数据不再显示伪真实观测时间。
- 熔断期间“下次检查”改为真实恢复探测时间，避免显示已经过期的缓存刷新时间。
- 修复项目创建轨迹把初始置信度和状态误当成 before/after 对象、显示 `undefined` 的问题；异常事件详情会被安全忽略。
- macOS 隔离数据进程显式使用 certifi 可信 CA bundle，修复 FR/FDR 证书链识别问题，继续保持完整 TLS 校验。
- 研究项目在 1280 像素级视口改为单栏详情，项目列表限高滚动，避免右侧正文被压缩和左侧大面积空白。
- 后端回归增至 68 项，前端回归增至 16 项；生产构建、Python 依赖检查和生产依赖审计通过。

## v0.6.1 — 市场投研联动版

- 投研首页新增“市场研究线索”，统一提炼资金利率、10Y 国债、期限利差、国债期货和可信现券异动，并展示观察时间、来源、质量分和显著程度。
- 新增可信市场快照：用户可选择线索和目标研究项目，服务端二次校验后把当时的中国固收市场状态保存为本地证据。
- 演示数据、质量分低于 60、不可用数据、异常隔离现券和缺失可信代码的现券不得进入正式市场快照。
- 研究项目新增观点轨迹，自动记录项目创建、字段更新、状态与置信度前后值、证据新增和证据移除；旧项目自动补充基线事件。
- 项目证据详情支持查看市场状态、快照时间、逐项市场信号、来源和用户备注；删除证据后同步刷新观点轨迹。
- 新增 `GET /research/market-brief` 与 `POST /research/projects/{id}/market-snapshot` 接口，以及 `research_project_activity` 本地表。
- FRED 继续维持请求级处理和引用证据边界；本版不扩大 FRED 落盘范围。
- 为控制小版本风险，World Bank 主权比较和信用观察 R1 调整到后续独立版本。
- 后端回归增至 61 项，前端回归增至 10 项；生产构建、Python 依赖检查、启动脚本语法和生产依赖审计通过。

## v0.6.0.1 — 可信校准版

- 按 FRED 当前完整条款校准数据边界：FRED 观测值改为请求级处理，不再写入 SQLite、后台预抓取或本地缓存；用户主动 CSV 导出继续保留。
- 升级时自动清理旧 FRED 宏观缓存，并把旧版含数值的冻结证据迁移为不含观测值的引用证据；新增证据大小上限和服务端防落盘校验。
- 研究证据新增详情查看、来源链接、删除确认和“以当前官方版本重新打开”；产品文案不再把 FRED 引用证据描述为冻结数据版本。
- 修复缺失日期被静默删除；原值、单期变化、归一化和 Z-Score 均保持空值边界，多序列图表继续以 `null` 对齐。
- 同比改用 FRED 官方 observations `units=pc1` 口径，不再使用 252/52/12/4 行固定回看。
- 图表响应与 CSV 补齐逐点 realtime vintage、抓取时间、实际来源机构、原始来源链接、变换来源、使用条款和 FRED 非背书声明。
- API Key 改为“验证成功后保存”；错误 Key 或连接失败会回滚原凭据，状态拆分为未配置、待验证、验证成功和验证失败。
- Keychain 写入不再把 Key 放入 `security` 命令参数；使用脱离终端会话的标准输入传递。
- FRED 新增 401、429、5xx 和网络错误分类、指数退避与连续失败熔断；健康页如实显示“线程池网络请求”和“无持久缓存”。
- 发布日历改为明确的前 14 天至后 45 天窗口并自动分页，避免近期开启项被 1000 条上限截断。
- 修复投研首页 UTC 日期在上海 00:00–08:00 的复盘偏差；自选类型改为中文，移除操作增加确认和错误反馈。
- macOS 启动器版本升级到 0.6.0.1，并能识别“8765 端口已占用但健康检查无响应”的旧进程。
- 后端回归增至 59 项，前端回归增至 9 项；真实 FRED 联调覆盖 DGS10、官方同比、双序列、发布日历、错误 Key 回滚和无观测值落盘。

## v0.6.0 — 宏观固收投研工作台基础版

- 新增投研首页，以“发现问题—保存证据—形成观点—定期复盘”为主线，汇总进行中研究、待复盘事项、统一自选和宏观缓存准备度。
- 新增本地研究项目，支持研究问题、初始假设、研究状态、主观置信度、期限、标签、结论和下次复盘日期。
- 新增冻结证据模型；图表数据、来源元信息、观察区间、变换方法和抓取版本随项目保存，后续官方修订不会静默改写历史证据。
- 直连 FRED 官方 API，首期提供 20 条美国政策、流动性、国债利率、通胀、实际利率、就业和增长精选指标。
- FRED API Key 使用用户本人凭据并保存至 macOS Keychain；环境变量可作为开发回退，Key 不进入数据库、日志、API 响应或导出文件。
- 新增宏观数据库与发布日历，展示指标频率、单位、季调、数据版本、官方更新时间、原始链接和 FRED 非背书声明。
- 新增图表研究室，支持最多 6 条序列、1–50 年区间、原值、单期变化、同比、归一化和 Z-Score，支持 CSV 与研究证据保存。
- 自选扩展为证券与宏观指标统一模型，新增分组、标签、研究状态、置顶、备注和复盘日期；宏观自选不会进入证券阈值提醒。
- 全局搜索新增 FRED 精选指标；每日复盘新增进行中项目、待复盘项目、自选宏观指标与宏观缓存摘要。
- FRED 使用独立缓存、低频调度和健康状态；未配置 Key 时明确降级，已有缓存仍可离线研究，故障不影响 AKShare 行情主链路。
- SQLite 执行向后兼容迁移，保留既有自选、提醒、触发历史和日报归档；清理缓存不会删除研究项目或冻结证据。
- 新增研究项目、证据、统一自选、FRED 缓存、数据版本、宏观变换和 API 生命周期测试。

## v0.5.1 — 可信稳定修复版

- 行情后台刷新改为固定两路工作队列并设置队列上限；页面刷新优先立即返回现有快照，再由后台去重更新，避免首页刷新形成无界并发。
- 后台调度会跳过仍处于熔断冷却期的数据源，不再重复累计失败次数；熔断期间保留最近一次真实根因，便于定位源头故障。
- 每日复盘可信度改为按资金、曲线、现券、期货和发行事件五个核心源加权；仅缓存或未检查的数据不能再得到“高可信”，并可展开查看各源贡献。
- 缺失可信代码或被数据契约判定为异常的现券、转债不再允许加入自选，避免污染提醒与后续研究链路。
- 保存视图新增版本化存储、字段校验、损坏项隔离和同名覆盖提示；筛选结果缩小时自动校正分页，并补充一键重置筛选。
- 复盘归档加载失败时清除旧报告，避免把上一份内容误认为当前结果；归档日期去重，失败后可直接返回今日复盘。
- 数据健康页正确展示 `0` 分和 `0 ms`，清理缓存前明确说明不会删除自选、提醒、历史与归档。
- 全局搜索与提醒抽屉补齐焦点约束和关闭后的焦点归还；图表增加读屏说明，错误/状态提示增加语义角色，并支持减少动态效果偏好。
- 新增前端保存视图、分页边界和异常标的自选保护测试；后端新增刷新队列、熔断根因和复盘可信度回归测试。

## v0.5.0 — 日常研究闭环版

- 每日固收复盘新增盘中快报、收盘复盘和最近快照口径，展示报告日期、综合可信度、可用数据源和异常隔离数量。
- 每日复盘自动归档至本地 SQLite，支持按日期离线回看；发行事件拆分为“待发行”和“近期已发行”。
- FR、FDR、LPR 拆分为三个独立适配器、缓存和健康状态，单一扩展源失败不再拖累其他指标或 Shibor 核心数据。
- 启动预热改为按核心优先级两路分批执行，避免慢速可选源占满队列；健康状态新增预计下次检查时间。
- 数据健康中心改为摘要信息加可展开运行详情，新增缓存可用、可信等级、异常隔离和独立资金源状态。
- 曲线历史明确展示实际覆盖起止日期与交易日数量，避免短历史被误认为一年数据缺失。
- 现券和可转债大表改为每页 50 条；所有筛选控件补充可访问名称，降低 DOM 规模并改善键盘/读屏使用。
- 保存视图取消原生命名弹窗，改为内联命名、载入、覆盖和删除；损坏的本地视图数据会安全回退为空列表。
- 提醒中心补齐可转债涨跌幅，新增指标单位、当前值试算、每日触发上限和跨午夜静默时段。
- 提醒规则数据库自动迁移旧表结构，保留既有规则；新增日报归档、API 生命周期、提醒试算和独立资金源测试。


## v0.4.1 — 可信修复版

- 修复服务重启命中新鲜持久缓存时健康状态仍为“未检查”的问题；新增“缓存可用”状态，并幂等恢复搜索索引与研究历史。
- 持久缓存升级改为向后兼容迁移，合法旧缓存不再因 schema 版本变化被直接删除。
- 新增 `trusted / partial / suspicious / unavailable` 数据可信等级和异常记录计数。
- 建立现券、期货、可转债和资金利率的基础数据契约；异常记录保持可见，但不进入日报榜单、历史和提醒。
- 现券不再使用名称冒充代码；缺失类型时按名称推断国债、政策金融债、同业存单、金融债和信用债。
- 可转债备用源过滤零价、无效代码和疑似失效标的，不再把无日期的 `ticktime` 拼成当天观察时间。
- 可转债价格备用数据质量分上限调整为 60，并明确标记为“部分可用”。
- 数据事件写入失败会进入活动状态指标，不再完全静默。
- 后端回归增加缓存恢复、旧缓存迁移、异常隔离和备用行情可信时间测试。

## v0.4.0 — 研究增强版

- 新增 SQLite 轻量历史研究序列；仅写入真实、未过期数据，滚动保留 400 天。
- 收益率曲线源回填最近交易日，曲线页新增 30/90/365 天利差趋势。
- 新增“每日固收复盘”，汇总资金、曲线、国债期货、现券异动、发行事件和数据可信状态，支持 Markdown 与打印 PDF。
- 现券新增收益率、异动和排序组合筛选；可转债新增价格、溢价、双低、YTM 筛选；两者均支持本地保存视图和 CSV 导出。
- 连续数据源失败自动进入熔断，冷却后半开探测；数据健康页展示熔断状态和下次重试时间。
- FR/FDR/LPR 改为扩展隔离进程抓取，失败不会拖累 Shibor 核心数据。
- 可转债比价主源不可用时自动切换新浪价格备用源，缺失的溢价率、双低和 YTM 明确标注。
- 提醒规则新增最近判断值、是否满足和评估时间，便于验证规则是否正常执行。
- 后端回归用例扩展到历史可信边界、序列查询、故障熔断、备用源和每日复盘。

## v0.3.0 — 稳定研究版

- AKShare 适配器改为独立子进程执行，最多两路并发；超时任务可强制终止，native 崩溃与主服务隔离。
- 新增启动预热和后台刷新调度；交易中按盘中 TTL 更新，收盘及非交易日自动降低频率。
- 新增 AKShare 交易日历适配器，区分交易中、已收盘、非交易日和日频数据。
- 每份数据新增实时/快照/缓存/过期/演示状态、质量评分和质量问题。
- 数据健康页展示刷新进度、市场状态、质量评分和隔离执行模式。
- 提醒改为条件首次满足或重新穿越阈值时触发，并由后台刷新驱动。
- 曲线页新增 10Y–1Y、10Y–5Y、30Y–10Y 利差与 10Y 日变动。
- 现券页新增债券类型筛选和 CSV 导出。
- 新增进程并发、进程超时、市场时段、交易日历、质量评分和阈值穿越测试。

## v0.2.1 — 并发稳定性修复

- 修复首页多数据源并发刷新触发 `libmini_racer` 致命错误、导致整个后端退出的问题。
- 所有 AKShare 调用改为同一个守护线程串行执行，并区分排队超时与数据源执行超时。
- 移除会遗留并发 AKShare 线程的可选增强抓取；FR/FDR/LPR 与独立强赎关联暂缓，页面明确显示缺失状态。
- 冷启动前端等待上限调整为 75 秒，以覆盖安全排队时间。
- 现券及可转债源没有可信市场时间时不再使用本地抓取时间冒充观测时间。
- 新增数据源串行执行和观测时间语义测试；回归用例达到 18 项。

## v0.2.0 — 可信可用版

- 新增 SQLite 最近真实行情缓存，重启后仍可使用；过期数据先展示、后台刷新。
- 数据适配器加入超时、有限重试、错误脱敏、独立刷新锁和单数据源重试。
- 数据健康页新增缓存年龄、持久化状态、连续失败次数和同步状态。
- 全局搜索支持现券、国债期货、可转债的代码、名称和别名检索。
- 自选状态在列表、按钮和自选页之间即时同步。
- 提醒规则真正执行，新增暂停、删除、冷却期、未读计数、触发历史与 macOS 通知。
- 演示数据与过期缓存被明确禁止触发提醒。
- 国债期货按候选季月合约的成交量选择活跃合约，不再依赖固定日期切换。
- 可转债接入强赎状态关联；不可用时明确显示“数据未接入”。
- 固收计算器新增 ACT/365、ACT/ACT、30/360 计息日规则，并修复本地日期受 UTC 偏移影响的问题。
- 图表代码按需加载，生产首屏 JavaScript 从约 786 KB 降至约 249 KB。
- 后端回归用例扩展到缓存、搜索、提醒可信边界和计息口径。

### 已知边界

- “信用观察”仍是 Beta 数据验证入口，尚未作为正式信用分析模块发布。
- 数据来自公开接口，不是交易所授权实时行情；仍不适合作为下单、估值入账或风控限额的唯一依据。
- 系统通知仅在应用运行且取得新的健康行情时触发，不是后台常驻行情服务。
